User (UserID (PK), FirstName, LastName, BirthDate, Email (unique), PasswordHash, Gender)
Organizer (OrganizerID (PK), OrganizationName, Address)
Referee (RefereeID (PK), QualificationLevel, Availability)
Tournament (TournamentID (PK), OrganizerID (FK → Organizer), Type, AgeLimitation, GenderLimitation, StartDate)
Rankings (RankingID (PK), TournamentID (FK → Tournament), UserID (FK → User), position)
MatchResult (MatchResultID (PK), MatchID (FK → Match), ScoreText, RecordedByRefereeID (FK → Referee))
MatchResultWinner (MatchWinnerID (PK), MatchResultID (FK → MatchResult), UserID (FK → User))
Match (MatchID (PK), TournamentID (FK → Tournament), ScheduledStart, ScheduledEnd)
MatchPlayer (MatchPlayerID (PK), UserID (FK → User), MatchID (FK → Match))
MatchViewer (MatchViewerID (PK), UserID (FK → User), MatchID (FK → Match))
TournamentSubscription (SubscriptionID (PK), UserID (FK → User), TournamentID (FK → Tournament))
